{{- range .Values.datasourcePermissions }}
---
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaManifest
metadata:
  name: {{ .name }}
  labels:
    {{- include "grafana-admin-platform.matchLabels" $ | nindent 4 }}
    grafana-admin-platform: managed
    owner: {{ .owner | default "platform" }}
spec:
  instanceSelector:
    matchLabels:
      {{- include "grafana-admin-platform.matchLabels" $ | nindent 6 }}
  template:
    apiVersion: iam.grafana.app/v0alpha1
    kind: ResourcePermission
    metadata:
      name: {{ .manifestName }}
    spec:
      target:
        kind: DataSource
        name: {{ .datasource }}
      assignments:
        {{- range .assignments }}
        {{- if .team }}
        - target:
            kind: Team
            name: {{ .team }}
          permission: {{ .permission }}
        {{- else if .serviceAccount }}
        - target:
            kind: ServiceAccount
            name: {{ .serviceAccount }}
          permission: {{ .permission }}
        {{- end }}
        {{- end }}
{{- end }}
