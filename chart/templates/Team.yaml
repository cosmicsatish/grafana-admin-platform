{{- range .Values.teams }}
---
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaManifest
metadata:
  name: {{ .slug }}-team
  labels:
    {{- include "grafana-admin-platform.labels" $ | nindent 4 }}
    owner: {{ .owner | default "platform" }}
  {{- if .roles }}
  annotations:
    grafana.app/roles: {{ join "," .roles | quote }}
  {{- end }}
spec:
  {{- include "grafana-admin-platform.instanceSelector" $ | nindent 2 }}
  template:
    apiVersion: iam.grafana.app/v0alpha1
    kind: Team
    metadata:
      name: {{ .slug }}
    spec:
      name: {{ .name | quote }}
      {{- if .email }}
      email: {{ .email | quote }}
      {{- end }}
      {{- if .syncGroups }}
      sync:
        groups:
          {{- range .syncGroups }}
          - {{ . | quote }}
          {{- end }}
      {{- end }}
{{- end }}
